                   ┌────────────────────┐
                   │      Client        │
                   └────────┬───────────┘
                            ↓
         ┌────────────────────────────────────┐
         │   Python Agent Service (核心层)    │
         │  (FastAPI + OpenAI-compatible API) │
         │------------------------------------│
         │ 1. Session加载（Redis）            │
         │ 2. 意图判断（是否需要补全）         │
         │ 3. 并行检索调度（关键优化点🔥）     │
         │ 4. 上下文融合（Memory + RAG）      │
         │ 5. LLM推理                        │
         │ 6. Memory更新                     │
         └───────┬──────────────┬───────────┘
                 ↓              ↓
     ┌────────────────┐   ┌────────────────────┐
     │   Redis        │   │ PostgreSQL         │
     │ (Session缓存)  │   │ (历史对话持久化)   │
     └────────────────┘   └────────────────────┘
                 ↓
         ┌────────────────────┐
         │  Vector DB         │
         │ (Milvus / PGVector)│
         └────────────────────┘
                 ↓
         ┌────────────────────┐
         │ Java SpringBoot    │
         │ (业务系统层)       │
         │ - 用户/权限        │
         │ - API服务          │
         └────────────────────┘



用 async / 多线程并行完成：

    async def parallel_retrieval():
        rag_task = vector_search(query)
        history_task = history_search(query, user_id)
    
        rag_docs, history_docs = await gather(rag_task, history_task)
        return rag_docs, history_docs
    
                用户问题
                  ↓
       ┌──────────┼──────────┐
       ↓          ↓          ↓
    RAG检索   历史检索   Redis上下文
java负责用户登录以及日志等

- 请求阶段

 Step 0：客户端请求

POST /v1/chat/completions

Step 1：进入 Python Agent

FastAPI /OpenAI兼容接口

Step 2：并行检索（核心优化，同时发起三个请求：RAG检索、历史检索、Redis上下文）

Step 3：上下文构建（按照json schema的设计对之前的检索结果进行拼接）

- 调用 LLM（推理）后直接返回前端，同时写入redis

- 异步写回PostgreSQL（java端，http调用，后期Kafka 异步实现）